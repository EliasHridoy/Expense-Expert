services:
  expo:
    build: .
    ports:
      - "8081:8081"
    environment:
      EXPO_DEVTOOLS_LISTEN_ADDRESS: "0.0.0.0"
      CHOKIDAR_USEPOLLING: "true"
      METRO_ENABLE_POLLING: "true"
      CI: "1"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run web
