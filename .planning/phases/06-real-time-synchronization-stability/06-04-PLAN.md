---
phase: "06-real-time-synchronization-stability"
plan: "04"
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - expense-expert-rn/src/features/auth/context/AuthProvider.tsx
  - expense-expert-rn/__tests__/routes/app-layout.test.tsx
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "User logout in AuthProvider executes RealtimeSyncManager.teardownAll(), purging all active Firestore listeners and resetting memory state."
    - "All unit and integration test suites in __tests__/ pass with zero failures."
    - "Full TypeScript type check executes with zero errors."
    - "Web production bundle compiles successfully without platform-specific bundler errors."
  artifacts:
    - expense-expert-rn/src/features/auth/context/AuthProvider.tsx
    - expense-expert-rn/__tests__/routes/app-layout.test.tsx
  key_links:
    - "AuthProvider coordinates listener teardown on user sign-out to prevent permission errors and memory leaks."
    - "CI validation commands (npm test, npx tsc, expo export) confirm production readiness across web and mobile."
---

# Plan 06-04: End-to-End Multi-Platform Stability & Build Health Verification

## Overview
Perform system-wide stability verification: integrate complete Firestore listener teardown into `AuthProvider` upon user logout, verify multi-provider interactions in `app-layout.test.tsx`, execute the full test suite, perform strict TypeScript validation, and verify web production export.

---

<tasks>

<task id="06-04-01">
  <name>Integrate Auth Logout Teardown and Route Integration Tests</name>
  <description>
    Ensure `AuthProvider` calls `RealtimeSyncManager.teardownAll()` on user logout or session termination, and create layout integration test verifying provider pipeline mounting.
  </description>
  <read_first>
    - /mnt/5cf2a800-97fa-463a-878d-37bb8b42ecdb/Pet Project/Expense Expert/expense-expert-rn/src/features/auth/context/AuthProvider.tsx
    - /mnt/5cf2a800-97fa-463a-878d-37bb8b42ecdb/Pet Project/Expense Expert/expense-expert-rn/src/features/sync/services/RealtimeSyncManager.ts
    - /mnt/5cf2a800-97fa-463a-878d-37bb8b42ecdb/Pet Project/Expense Expert/expense-expert-rn/app/(app)/_layout.tsx
  </read_first>
  <acceptance_criteria>
    - `AuthProvider.tsx` calls `RealtimeSyncManager.teardownAll()` whenever the authenticated user becomes null.
    - `__tests__/routes/app-layout.test.tsx` renders the full `AppLayoutGroup` without throwing errors.
    - `__tests__/routes/app-layout.test.tsx` passes 100%.
  </acceptance_criteria>
  <action>
    Update `src/features/auth/context/AuthProvider.tsx`:
    - Import `RealtimeSyncManager`.
    - In `onAuthStateChanged(auth, (firebaseUser) => { ... })`:
      - If `!firebaseUser`, invoke `RealtimeSyncManager.teardownAll()`.
    - In `logout` function:
      - Invoke `RealtimeSyncManager.teardownAll()`.

    Create `__tests__/routes/app-layout.test.tsx`:
    - Render `AppLayoutGroup` with mocked Firebase & network contexts.
    - Assert that all nested providers mount without crash or unhandled rejection.
  </action>
  <verify>
    <automated>npm test -- __tests__/routes/app-layout.test.tsx</automated>
  </verify>
  <done>
    Auth logout teardown guarantees zero memory leaks across user sessions and layout mounting is verified.
  </done>
</task>

<task id="06-04-02">
  <name>Full Test Suite and TypeScript Typecheck Verification</name>
  <description>
    Run the entire test suite across all features and execute a comprehensive TypeScript compile check.
  </description>
  <read_first>
    - /mnt/5cf2a800-97fa-463a-878d-37bb8b42ecdb/Pet Project/Expense Expert/expense-expert-rn/package.json
  </read_first>
  <acceptance_criteria>
    - `npm test` runs all test suites with 0 failures.
    - `npx tsc --noEmit` passes with 0 type errors.
  </acceptance_criteria>
  <action>
    Run `npm test` inside `expense-expert-rn`. Fix any regressions if found.
    Run `npx tsc --noEmit` inside `expense-expert-rn`. Fix any type issues if found.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <done>
    Entire test suite and TypeScript type checking pass cleanly.
  </done>
</task>

<task id="06-04-03">
  <name>Web Production Export & Multi-Platform Build Verification</name>
  <description>
    Verify that the multi-platform codebase builds cleanly for web production without bundler errors.
  </description>
  <read_first>
    - /mnt/5cf2a800-97fa-463a-878d-37bb8b42ecdb/Pet Project/Expense Expert/expense-expert-rn/package.json
  </read_first>
  <acceptance_criteria>
    - `npx expo export -p web` or `npm run build:web` succeeds with exit code 0.
    - Generated output files exist without syntax or missing asset errors.
  </acceptance_criteria>
  <action>
    Run production web export from `expense-expert-rn`:
    `npx expo export -p web`
  </action>
  <verify>
    <automated>npx expo export -p web</automated>
  </verify>
  <done>
    Web export completes with zero errors, confirming cross-platform bundling integrity.
  </done>
</task>

</tasks>

---

<verification>
Run all validation gates:
```bash
npm test
npx tsc --noEmit
npx expo export -p web
```
</verification>

<success_criteria>
- Multi-user listener isolation and logout teardown are verified.
- Complete unit test suite is 100% green.
- TypeScript compiler reports zero errors.
- Production web bundle is successfully generated.
</success_criteria>
