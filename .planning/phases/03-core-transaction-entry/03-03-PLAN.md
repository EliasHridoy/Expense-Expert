---
phase: 03-core-transaction-entry
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - expense-expert-rn/src/features/expenses/components/AmountInput.tsx
  - expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx
  - expense-expert-rn/src/features/expenses/components/DateSelector.tsx
  - expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx
  - expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx
  - expense-expert-rn/__tests__/features/expenses/ExpenseForm.test.tsx
autonomous: true
requirements:
  - TXN-01
  - TXN-02
must_haves:
  truths:
    - "AmountInput renders a large-format currency input with safe integer cent sanitization and zero cursor drift"
    - "CategoryCardPicker provides an interactive grid of emoji category cards with active selection indicators"
    - "DateSelector provides cross-platform date selection defaulting to today"
    - "ExpenseForm implements the 3-step wizard (Step 1: Amount & Category, Step 2: Title & Date, Step 3: Note & Summary Card)"
    - "Step 1 validates amount > 0 and category selected before permitting progression to Step 2"
    - "Summary card in Step 3 displays formatted currency, selected category emoji, title, and formatted date"
    - "Automated component unit tests verify wizard navigation, input validation, editing pre-fill, and submission"
  artifacts:
    - path: "expense-expert-rn/src/features/expenses/components/AmountInput.tsx"
      provides: "Large-format currency input with cent-level formatting"
      contains: "export const AmountInput"
    - path: "expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx"
      provides: "Grid of category cards with emoji icons"
      contains: "export const CategoryCardPicker"
    - path: "expense-expert-rn/src/features/expenses/components/DateSelector.tsx"
      provides: "Cross-platform date picker component"
      contains: "export const DateSelector"
    - path: "expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx"
      provides: "Status banner displaying pending offline sync items"
      contains: "export const OfflineSyncBanner"
    - path: "expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx"
      provides: "3-step wizard expense creation and editing form"
      contains: "export const ExpenseForm"
    - path: "expense-expert-rn/__tests__/features/expenses/ExpenseForm.test.tsx"
      provides: "Component interaction and validation test suite for ExpenseForm"
      contains: "describe('ExpenseForm'"
  key_links:
    - from: "expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx"
      to: "expense-expert-rn/src/features/expenses/hooks/useExpenses.ts"
      via: "calls addExpense and updateExpense on form submit"
      pattern: "useExpenses\\(\\)"
    - from: "expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx"
      to: "expense-expert-rn/src/features/expenses/utils/currency.util.ts"
      via: "formats cents and validates amounts"
      pattern: "formatCents"
---

<objective>
Build the responsive, cross-platform 3-step transaction entry wizard UI matching the Angular application's design system using NativeWind v4, complete with large-format amount input, category card grid, smart title suggestion pills, universal date selector, review summary card, and automated component test suites.

Purpose: Provide an intuitive, low-friction, and error-resilient expense entry experience across mobile and web viewports.
Output: `AmountInput`, `CategoryCardPicker`, `DateSelector`, `OfflineSyncBanner`, `ExpenseForm`, and component tests.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-transaction-entry/03-RESEARCH.md
@.planning/phases/03-core-transaction-entry/03-VALIDATION.md
@expense-expert/src/app/features/expenses/expense-form/expense-form.component.ts
@expense-expert-rn/src/features/expenses/types/expense.types.ts
@expense-expert-rn/src/features/expenses/types/category.types.ts
@expense-expert-rn/src/features/expenses/utils/currency.util.ts
@expense-expert-rn/src/features/expenses/hooks/useExpenses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AmountInput, CategoryCardPicker, DateSelector, and OfflineSyncBanner sub-components</name>
  <files>expense-expert-rn/src/features/expenses/components/AmountInput.tsx, expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx, expense-expert-rn/src/features/expenses/components/DateSelector.tsx, expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx</files>
  <read_first>expense-expert/src/app/features/expenses/expense-form/expense-form.component.ts, .planning/phases/03-core-transaction-entry/03-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `src/features/expenses/components/AmountInput.tsx`:
   - Accepts `value: string`, `onChangeText: (text: string) => void`, `error?: string`.
   - Renders a styled card container (`bg-white dark:bg-slate-800 rounded-3xl p-6 shadow-sm border border-slate-100 dark:border-slate-700/50`).
   - Displays label "Amount" centered uppercase tracking-wider.
   - Large numeric input (`text-3xl sm:text-4xl font-bold text-center text-slate-900 dark:text-slate-100`) with "$" prefix indicator.
   - Input props: `keyboardType="decimal-pad"`, `placeholder="0.00"`, `selectTextOnFocus`.

2. Create `src/features/expenses/components/CategoryCardPicker.tsx`:
   - Accepts `selectedValue: string`, `onSelect: (category: string) => void`, `customCategories?: CategoryItem[]`.
   - Merges `EXPENSE_CATEGORIES` with `customCategories`.
   - Renders a responsive grid (`flex-row flex-wrap justify-between gap-y-3`).
   - Category card item: `w-[31%] sm:w-[23%] aspect-square rounded-2xl items-center justify-center p-2 border-2`.
   - Highlights active selection with `border-indigo-600 bg-indigo-50 dark:bg-indigo-950/40 text-indigo-600`.
   - Displays category emoji and truncated label.

3. Create `src/features/expenses/components/DateSelector.tsx`:
   - Accepts `value: string` (YYYY-MM-DD), `onChange: (dateStr: string) => void`.
   - On web: renders native `<input type="date" value={value} onChange={...} />` with styled Tailwind classes matching the design system.
   - On native / cross-platform: renders styled container with date icon, displaying formatted display date (e.g. "Aug 23, 2026"), and text input or quick-select date buttons (Today, Yesterday).

4. Create `src/features/expenses/components/OfflineSyncBanner.tsx`:
   - Accepts `pendingCount: number`, `isOnline: boolean`, `onSyncNow?: () => void`.
   - If `isOnline && pendingCount === 0`, returns null.
   - If `!isOnline`: renders an amber offline banner ("Offline Mode • X transactions queued locally").
   - If `isOnline && pendingCount > 0`: renders an indigo/blue sync in progress or sync ready banner ("X transactions pending sync", "Sync Now" button).
  </action>
  <verify>
    Verify all 4 sub-components compile without errors via `npx tsc --noEmit`.
  </verify>
  <acceptance_criteria>
    - `AmountInput.tsx` exports `AmountInput`
    - `CategoryCardPicker.tsx` exports `CategoryCardPicker`
    - `DateSelector.tsx` exports `DateSelector`
    - `OfflineSyncBanner.tsx` exports `OfflineSyncBanner`
  </acceptance_criteria>
  <done>All 4 expense entry sub-components created with NativeWind styling.</done>
</task>

<task type="auto">
  <name>Task 2: Build ExpenseForm 3-step wizard with validation, title suggestions, summary card, and responsive layout</name>
  <files>expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx</files>
  <read_first>expense-expert/src/app/features/expenses/expense-form/expense-form.component.ts, expense-expert-rn/src/features/expenses/hooks/useExpenses.ts, .planning/phases/03-core-transaction-entry/03-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `src/features/expenses/components/ExpenseForm.tsx`:
   - Props:
     - `initialData?: Partial<Expense>` (for edit mode)
     - `onSuccess?: (expense: Expense) => void`
     - `onCancel?: () => void`
   - State:
     - `currentStep`: `1 | 2 | 3` (default 1)
     - `amount`: string (e.g. `""` or `initialData?.amount?.toString() || ""`)
     - `category`: string (default `ExpenseCategory.Food` or `initialData?.category`)
     - `title`: string (default `initialData?.title || ""`)
     - `date`: string (YYYY-MM-DD, default `toDateInputValue(new Date())` or `initialData?.date`)
     - `description`: string (default `initialData?.description || ""`)
     - `isLoan`: boolean (default `initialData?.isLoan || false`)
     - `loanPersonId`: string | null (default `initialData?.loanPersonId || null`)
     - `isSubmitting`: boolean
     - `errorMessage`: string | null
   - Suggestions: `['Grocery', 'Coffee', 'Transport', 'Utilities', 'Shopping', 'Dinner', 'Medicine']`.
   - Wizard Navigation:
     - Step 1 validation: `canProceedStep1 = toCents(amount) > 0 && !!category`.
     - Step 2 validation: `canProceedStep2 = title.trim().length > 0 && !!date`.
     - Step 3: Review summary card + optional notes & loan metadata.
     - Header: displays Back button (if step > 1) or Cancel button, title ("Add Expense" vs "Edit Expense"), and 3-step progress bar.
     - Step 1 (Amount & Category): Renders `AmountInput` and `CategoryCardPicker`.
     - Step 2 (Title & Date): Renders Title text input, horizontal scrollable Suggestion Pills (tapping sets title), and `DateSelector`.
     - Step 3 (Details & Summary): Renders Note textarea, and Review Summary Card showing formatted amount (via `formatCents(toCents(amount))`), title, category emoji/name, and formatted date.
     - Sticky Bottom Action Bar:
       - Step 1 & 2: "Continue" button (disabled if step invalid).
       - Step 3: "Save Expense" / "Update Expense" button (shows spinner when saving).
   - Form Submission:
     - On submit: sets `isSubmitting(true)`.
     - Calls `addExpense` or `updateExpense` from `useExpenses()`.
     - On success: triggers `onSuccess?.(result)`.
     - On error: sets `errorMessage`.
  </action>
  <verify>
    Verify `ExpenseForm.tsx` compiles and exports `ExpenseForm`.
  </verify>
  <acceptance_criteria>
    - `ExpenseForm.tsx` exports `ExpenseForm`
    - Form validates step 1 (amount > 0 and category) and step 2 (title and date)
    - Summary card renders formatted dollar value and transaction details in step 3
    - Handles both create and edit modes
  </acceptance_criteria>
  <done>ExpenseForm 3-step wizard component fully implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Create automated unit and interaction tests for ExpenseForm</name>
  <files>expense-expert-rn/__tests__/features/expenses/ExpenseForm.test.tsx</files>
  <read_first>expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx, expense-expert-rn/src/features/expenses/hooks/useExpenses.ts</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `__tests__/features/expenses/ExpenseForm.test.tsx`:
   - Mock `useExpenses` hook and `expo-router`.
   - Test Step 1:
     - Renders Amount input and Category picker.
     - "Continue" button is disabled when amount is empty or 0.
     - Entering amount `45.50` and selecting a category enables "Continue".
     - Clicking "Continue" advances to Step 2.
   - Test Step 2:
     - Renders Title input and date input.
     - Clicking suggestion pill ("Grocery") populates Title field.
     - "Continue" button is disabled when title is blank.
     - Clicking "Continue" advances to Step 3.
   - Test Step 3:
     - Renders Note textarea and Summary card with formatted `$45.50`.
     - Clicking "Save Expense" invokes `addExpense` with expected DTO payload and calls `onSuccess`.
   - Test Edit Mode:
     - Passing `initialData` pre-fills fields and renders "Update Expense".
  </action>
  <verify>
    Run `npm test -- __tests__/features/expenses/ExpenseForm.test.tsx` to verify component test suite.
  </verify>
  <acceptance_criteria>
    - `__tests__/features/expenses/ExpenseForm.test.tsx` executes and passes all test cases
    - Step transitions, validation guards, and submission are fully covered
  </acceptance_criteria>
  <done>Component test suite for ExpenseForm created and passing cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- __tests__/features/expenses/ExpenseForm.test.tsx` passes.
- [ ] `npx tsc --noEmit` completes with 0 errors.
</verification>

<success_criteria>
- All tasks completed successfully.
- 3-step wizard UX accurately reproduces the Angular app flow and Tailwind aesthetics.
- Input validation and integer cent conversion protect financial data integrity.
- Component test suite passes with 100% assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction-entry/03-03-SUMMARY.md`
</output>
