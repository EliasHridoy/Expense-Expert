---
phase: 03-core-transaction-entry
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - expense-expert-rn/app/(app)/_layout.tsx
  - expense-expert-rn/app/(app)/expenses/new.tsx
  - expense-expert-rn/app/(app)/expenses/[id].tsx
  - expense-expert-rn/app/(app)/index.tsx
autonomous: true
requirements:
  - TXN-01
  - TXN-02
  - TXN-03
must_haves:
  truths:
    - "Protected route group (app)/_layout.tsx mounts ExpenseProvider to supply expense state and offline sync across all authenticated views"
    - "Route /expenses/new renders ExpenseForm and navigates back to /(app) upon successful expense creation"
    - "Route /expenses/[id] loads existing expense by ID, populates ExpenseForm in edit mode, and saves updates"
    - "Dashboard /(app)/index.tsx displays Quick Add Expense button, OfflineSyncBanner with pending item count, and recent expenses list"
    - "Full test suite, strict TypeScript type check, and web production build execute successfully with 0 errors"
  artifacts:
    - path: "expense-expert-rn/app/(app)/_layout.tsx"
      provides: "Protected layout wrapping sub-routes in ExpenseProvider"
      contains: "ExpenseProvider"
    - path: "expense-expert-rn/app/(app)/expenses/new.tsx"
      provides: "Screen route for creating new expenses"
      contains: "ExpenseForm"
    - path: "expense-expert-rn/app/(app)/expenses/[id].tsx"
      provides: "Screen route for editing existing expenses by ID"
      contains: "ExpenseForm"
    - path: "expense-expert-rn/app/(app)/index.tsx"
      provides: "Dashboard with Quick Add Expense button, offline sync banner, and transaction items"
      contains: "router.push('/expenses/new')"
  key_links:
    - from: "expense-expert-rn/app/(app)/_layout.tsx"
      to: "expense-expert-rn/src/features/expenses/context/ExpenseProvider.tsx"
      via: "wraps protected app stack in ExpenseProvider"
      pattern: "<ExpenseProvider>"
    - from: "expense-expert-rn/app/(app)/expenses/new.tsx"
      to: "expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx"
      via: "renders ExpenseForm for new expense creation"
      pattern: "<ExpenseForm"
    - from: "expense-expert-rn/app/(app)/index.tsx"
      to: "expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx"
      via: "renders offline connectivity and pending sync banner"
      pattern: "<OfflineSyncBanner"
---

<objective>
Wire up the complete transaction entry routing architecture in Expo Router v4: wrap the protected app group with `ExpenseProvider`, create `/expenses/new` and `/expenses/[id]` screen routes, enhance the authenticated home dashboard with Quick Add Expense actions and offline sync monitoring, and execute full end-to-end automated validation across tests, type checks, and production web builds.

Purpose: Provide frictionless navigation, route integration, and verified production stability for core transaction entry across web and mobile.
Output: Expo Router screens `(app)/expenses/new.tsx`, `(app)/expenses/[id].tsx`, updated `_layout.tsx` and `index.tsx`, and green validation passes.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-transaction-entry/03-RESEARCH.md
@.planning/phases/03-core-transaction-entry/03-VALIDATION.md
@expense-expert-rn/src/features/expenses/context/ExpenseProvider.tsx
@expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx
@expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx
@expense-expert-rn/src/features/expenses/hooks/useExpenses.ts
@expense-expert-rn/app/(app)/_layout.tsx
@expense-expert-rn/app/(app)/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount ExpenseProvider in (app)/_layout.tsx and create expenses/new and expenses/[id] screen routes</name>
  <files>expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/app/(app)/expenses/new.tsx, expense-expert-rn/app/(app)/expenses/[id].tsx</files>
  <read_first>expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/src/features/expenses/context/ExpenseProvider.tsx, expense-expert-rn/src/features/expenses/components/ExpenseForm.tsx</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Update `app/(app)/_layout.tsx`:
   - Import `ExpenseProvider` from `../../src/features/expenses/context/ExpenseProvider`.
   - Wrap `<Stack screenOptions={{ headerShown: false }} />` with `<ExpenseProvider>`.

2. Create `app/(app)/expenses/new.tsx`:
   - Import `useRouter` from `expo-router`.
   - Import `SafeAreaView` from `react-native-safe-area-context`.
   - Import `ExpenseForm` from `../../../src/features/expenses/components/ExpenseForm`.
   - Implement `NewExpenseScreen`:
     - Renders `SafeAreaView` with `bg-slate-50 dark:bg-slate-900 flex-1`.
     - Renders `ExpenseForm` with:
       - `onSuccess={() => router.replace('/(app)')}`
       - `onCancel={() => router.back()}`

3. Create `app/(app)/expenses/[id].tsx`:
   - Import `useLocalSearchParams`, `useRouter` from `expo-router`.
   - Import `useExpenses` hook.
   - Implement `EditExpenseScreen`:
     - Loads expense data using `getExpenseById(id)` from `useExpenses()`.
     - Displays loading indicator while fetching.
     - If expense not found, displays error and Back button.
     - Renders `ExpenseForm` with `initialData={expense}`, `onSuccess={() => router.replace('/(app)')}`, `onCancel={() => router.back()}`.
  </action>
  <verify>
    Verify `app/(app)/_layout.tsx`, `app/(app)/expenses/new.tsx`, and `app/(app)/expenses/[id].tsx` exist and compile cleanly.
  </verify>
  <acceptance_criteria>
    - `app/(app)/_layout.tsx` wraps protected routes in `ExpenseProvider`
    - `app/(app)/expenses/new.tsx` renders `ExpenseForm` and handles cancel/success navigation
    - `app/(app)/expenses/[id].tsx` fetches expense by ID and renders `ExpenseForm` in edit mode
  </acceptance_criteria>
  <done>Expense screen routes and ExpenseProvider layout integration created.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance dashboard (app)/index.tsx with Quick Add CTA, offline sync banner, and recent expenses list</name>
  <files>expense-expert-rn/app/(app)/index.tsx</files>
  <read_first>expense-expert-rn/app/(app)/index.tsx, expense-expert-rn/src/features/expenses/hooks/useExpenses.ts, expense-expert-rn/src/features/expenses/components/OfflineSyncBanner.tsx, expense-expert-rn/src/features/expenses/utils/currency.util.ts</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Update `app/(app)/index.tsx`:
   - Consume `useAuth()` to get user profile and `logout`.
   - Consume `useExpenses()` to get `expenses`, `pendingSyncCount`, `isOnline`, `isSyncing`, `syncQueue`.
   - Render:
     - Header with welcome message, user profile email/badge, and Logout button.
     - `<OfflineSyncBanner pendingCount={pendingSyncCount} isOnline={isOnline} onSyncNow={syncQueue} />`.
     - Quick Action Card with "Add New Expense" button (`onPress={() => router.push('/(app)/expenses/new')}`), styled with bold indigo action button (`+ Add Expense`).
     - Recent Transactions List:
       - Displays recent expense items showing title, category emoji, date, formatted currency amount (via `formatCents(expense.amountInCents)`), and sync badge (`pending` vs `synced`).
       - If expenses list is empty, displays friendly empty state ("No expenses recorded yet. Tap '+ Add Expense' to log your first transaction.").
       - Tapping an item navigates to `/(app)/expenses/${expense.id}`.
  </action>
  <verify>
    Verify `app/(app)/index.tsx` renders without errors and connects with `useExpenses`.
  </verify>
  <acceptance_criteria>
    - `app/(app)/index.tsx` displays user header, `OfflineSyncBanner`, Quick Add CTA, and recent transactions
    - Tapping Quick Add navigates to `/expenses/new`
    - Displays pending sync badge for offline created expenses
  </acceptance_criteria>
  <done>Dashboard updated with transaction entry actions and sync feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full automated validation suite, type checking, and web build verification</name>
  <files>expense-expert-rn/package.json</files>
  <read_first>.planning/phases/03-core-transaction-entry/03-VALIDATION.md</read_first>
  <action>
Execute the comprehensive test and verification suite inside `expense-expert-rn/`:
1. Run all unit and integration test suites:
   `npm test`
2. Run strict TypeScript type check:
   `npx tsc --noEmit`
3. Run web production build to verify bundle compilation and web asset generation:
   `npm run build:web`
  </action>
  <verify>
    Verify `npm test`, `npx tsc --noEmit`, and `npm run build:web` all complete with exit code 0.
  </verify>
  <acceptance_criteria>
    - `npm test` runs all currency math, date, queue, service, and form test suites with 0 failures
    - `npx tsc --noEmit` produces 0 type errors
    - `npm run build:web` successfully produces production web bundle
  </acceptance_criteria>
  <done>Complete automated test suite, type check, and web build passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests cleanly.
- [ ] `npx tsc --noEmit` passes with 0 type errors.
- [ ] `npm run build:web` completes successfully.
- [ ] Routes `/expenses/new` and `/expenses/[id]` are accessible and functional.
</verification>

<success_criteria>
- All tasks completed successfully.
- Transaction entry route flow is fully connected to Expo Router.
- Network status changes trigger visual feedback and auto-sync.
- Automated validation passes with 100% success rate.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-transaction-entry/03-04-SUMMARY.md`
</output>
