---
phase: 04-categorization-budgeting
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - expense-expert-rn/app/(app)/_layout.tsx
  - expense-expert-rn/app/(app)/index.tsx
  - expense-expert-rn/app/(app)/budgets/index.tsx
  - expense-expert-rn/app/(app)/categories/index.tsx
  - expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx
  - expense-expert-rn/__tests__/routes/budget-routes.test.tsx
  - expense-expert-rn/__tests__/routes/category-routes.test.tsx
  - expense-expert-rn/__tests__/features/expenses/CategoryCardPicker.test.tsx
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
must_haves:
  truths:
    - "The app layout wraps all authenticated screens with CategoryProvider and BudgetProvider"
    - "Users can access and manage category budgets from a dedicated Budgets screen (/(app)/budgets)"
    - "Users can access and manage custom categories from a dedicated Categories screen (/(app)/categories)"
    - "The transaction entry wizard (CategoryCardPicker) dynamically presents custom categories created by the user"
    - "The main dashboard integrates transaction filtering (chips, date presets, search) and displays monthly budget progress overview"
    - "All automated test suites, TypeScript type checks, and web builds pass cleanly"
  artifacts:
    - path: "expense-expert-rn/app/(app)/_layout.tsx"
      provides: "Authenticated layout stack wrapping ExpenseProvider, CategoryProvider, and BudgetProvider"
      contains: "CategoryProvider"
    - path: "expense-expert-rn/app/(app)/budgets/index.tsx"
      provides: "Dedicated Monthly Category Budgets management screen"
      contains: "export default function BudgetsScreen"
    - path: "expense-expert-rn/app/(app)/categories/index.tsx"
      provides: "Dedicated Custom Categories screen"
      contains: "export default function CategoriesScreen"
    - path: "expense-expert-rn/app/(app)/index.tsx"
      provides: "Dashboard with integrated transaction filter controls, view modes, and budget summary widget"
      contains: "useTransactionFilters"
    - path: "expense-expert-rn/__tests__/routes/budget-routes.test.tsx"
      provides: "Automated route and integration tests for Budgets screen"
      contains: "describe('Budgets Screen'"
    - path: "expense-expert-rn/__tests__/routes/category-routes.test.tsx"
      provides: "Automated route and integration tests for Categories screen"
      contains: "describe('Categories Screen'"
  key_links:
    - from: "expense-expert-rn/app/(app)/_layout.tsx"
      to: "expense-expert-rn/src/features/categories/context/CategoryProvider.tsx"
      via: "wraps child stack in CategoryProvider"
      pattern: "<CategoryProvider>"
    - from: "expense-expert-rn/app/(app)/_layout.tsx"
      to: "expense-expert-rn/src/features/budgets/context/BudgetProvider.tsx"
      via: "wraps child stack in BudgetProvider"
      pattern: "<BudgetProvider>"
    - from: "expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx"
      to: "expense-expert-rn/src/features/categories/hooks/useCategories.ts"
      via: "useCategories to display custom categories dynamically in forms"
      pattern: "useCategories()"
---

<objective>
Integrate all Phase 4 features into the application routing structure, navigation flows, and dashboard (CAT-01, CAT-02, CAT-03): nest `CategoryProvider` and `BudgetProvider` in `(app)/_layout.tsx`, create dedicated screens for Budgets (`/(app)/budgets`) and Categories (`/(app)/categories`), connect the transaction filtering engine and budget progress meters into the main dashboard (`/(app)/index.tsx`), update `CategoryCardPicker` to dynamically consume custom categories, and verify everything with automated integration test suites, TypeScript compilation, and web build.

Purpose: Deliver a unified, seamless user experience for custom categories, multi-criteria transaction filtering, and monthly budget progress meters.
Output: Integrated routes, dashboard controls, budget screen, category screen, integration tests, and passing verification.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-categorization-budgeting/04-RESEARCH.md
@.planning/phases/04-categorization-budgeting/04-VALIDATION.md
@expense-expert-rn/app/(app)/_layout.tsx
@expense-expert-rn/app/(app)/index.tsx
@expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app layout with CategoryProvider and BudgetProvider, and update CategoryCardPicker</name>
  <files>expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx, expense-expert-rn/__tests__/features/expenses/CategoryCardPicker.test.tsx</files>
  <read_first>expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/src/features/expenses/components/CategoryCardPicker.tsx, expense-expert-rn/src/features/categories/hooks/useCategories.ts, .planning/phases/04-categorization-budgeting/04-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Update `app/(app)/_layout.tsx`:
   - Import `CategoryProvider` from `../../src/features/categories/context/CategoryProvider`.
   - Import `BudgetProvider` from `../../src/features/budgets/context/BudgetProvider`.
   - Wrap the `Stack` navigation hierarchy:
     ```tsx
     <ExpenseProvider>
       <CategoryProvider>
         <BudgetProvider>
           <Stack screenOptions={{ headerShown: false }}>
             <Stack.Screen name="index" />
             <Stack.Screen name="expenses/new" />
             <Stack.Screen name="expenses/[id]" />
             <Stack.Screen name="budgets/index" />
             <Stack.Screen name="categories/index" />
           </Stack>
         </BudgetProvider>
       </CategoryProvider>
     </ExpenseProvider>
     ```

2. Update `src/features/expenses/components/CategoryCardPicker.tsx`:
   - Use `useCategories()` hook to fetch `categories` (combining built-in and user-created custom categories).
   - If explicit `customCategories` prop is passed, merge them gracefully, otherwise use the active context categories.
   - Maintain active selection borders and accessible role attributes.

3. Update `__tests__/features/expenses/CategoryCardPicker.test.tsx`:
   - Verify built-in categories and custom categories from `CategoryProvider` render with emoji icons.
   - Verify selection triggers `onSelect` callback.
  </action>
  <verify>
    Run `npm test -- __tests__/features/expenses/CategoryCardPicker.test.tsx` to verify component rendering and selection.
  </verify>
  <acceptance_criteria>
    - `app/(app)/_layout.tsx` wraps child screens with `CategoryProvider` and `BudgetProvider`.
    - `CategoryCardPicker` dynamically shows user's custom categories in addition to built-ins.
    - Tests in `CategoryCardPicker.test.tsx` pass.
  </acceptance_criteria>
  <done>Layout providers connected and CategoryCardPicker wired to dynamic category context.</done>
</task>

<task type="auto">
  <name>Task 2: Build dedicated Budgets and Categories screens and update Dashboard</name>
  <files>expense-expert-rn/app/(app)/budgets/index.tsx, expense-expert-rn/app/(app)/categories/index.tsx, expense-expert-rn/app/(app)/index.tsx</files>
  <read_first>expense-expert-rn/app/(app)/index.tsx, expense-expert-rn/src/features/budgets/hooks/useBudgets.ts, expense-expert-rn/src/features/expenses/hooks/useTransactionFilters.ts, .planning/phases/04-categorization-budgeting/04-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `app/(app)/budgets/index.tsx`:
   - Header with title "Monthly Budgets", back button to dashboard, and "+ Set Budget" action button.
   - Month navigation selector (previous/next month buttons and current month display, e.g. "August 2026").
   - `BudgetSummaryCard` showing overall total limit vs spent for the selected month.
   - List of `CategoryBudgetCard` items for each configured category budget.
   - Empty state: displays illustration/message and "Set Your First Budget" button when no category budgets exist for the month.
   - `SetBudgetModal` for creating or editing category budgets.

2. Create `app/(app)/categories/index.tsx`:
   - Header with title "Manage Categories", back button, and "+ New Category" button.
   - List of built-in categories (with default badges and lock indicator explaining they are standard categories).
   - List of user-created custom categories with emoji icon, name, and delete button with confirmation dialog.
   - Add/edit category modal (`CategoryListModal`).

3. Enhance `app/(app)/index.tsx` (Dashboard):
   - Add top navigation / quick access shortcuts to **Budgets** (`/budgets`) and **Categories** (`/categories`).
   - Add a compact **Budget Summary Widget** showcasing active month's spending vs total budget limit with the `BudgetProgressBar` threshold indicator.
   - Integrate `ExpenseListHeader` / `FilterChips` / `ExpenseSearchBar` with `useTransactionFilters(expenses)` to filter the recent transactions list dynamically.
   - Render filtered & sorted transactions with support for List and Grid view modes and grouping by Category or Date.
   - Display active filter count and empty filter results state with a "Reset Filters" action button.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify all screen routes and components typecheck cleanly.
  </verify>
  <acceptance_criteria>
    - `app/(app)/budgets/index.tsx` renders monthly budget management UI with month selector, summary card, and category cards.
    - `app/(app)/categories/index.tsx` renders custom category management UI.
    - `app/(app)/index.tsx` includes filter controls, list/grid view toggling, and budget summary widget.
  </acceptance_criteria>
  <done>Budgets screen, Categories screen, and enhanced Dashboard created and wired.</done>
</task>

<task type="auto">
  <name>Task 3: Automated route tests and full suite verification</name>
  <files>expense-expert-rn/__tests__/routes/budget-routes.test.tsx, expense-expert-rn/__tests__/routes/category-routes.test.tsx</files>
  <read_first>expense-expert-rn/__tests__/routes/expense-routes.test.tsx, .planning/phases/04-categorization-budgeting/04-VALIDATION.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `__tests__/routes/budget-routes.test.tsx`:
   - Render `BudgetsScreen` within providers.
   - Verify budget summary card, category budget cards, and "+ Set Budget" modal opening.
   - Test setting a new category budget updates the display.
   - Test threshold warning states (<80%, 80-99%, >=100%) render appropriate color classes.

2. Create `__tests__/routes/category-routes.test.tsx`:
   - Render `CategoriesScreen` within providers.
   - Verify built-in categories and custom categories are displayed.
   - Test opening modal, selecting emoji, entering name, and submitting creates category.
   - Test deleting custom category.

3. Run full verification suite:
   - `npm test`
   - `npx tsc --noEmit`
   - `npm run build:web`
  </action>
  <verify>
    Run `npm test && npx tsc --noEmit && npm run build:web` to verify complete test pass, type safety, and web bundle compilation.
  </verify>
  <acceptance_criteria>
    - `__tests__/routes/budget-routes.test.tsx` and `__tests__/routes/category-routes.test.tsx` pass all test assertions.
    - `npm test` passes 100% across the entire test suite.
    - `npx tsc --noEmit` succeeds with zero errors.
    - `npm run build:web` compiles successfully without bundling errors.
  </acceptance_criteria>
  <done>Route tests and full verification pipeline completed successfully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes all tests across auth, expenses, categories, budgets, and routes.
- [ ] `npx tsc --noEmit` completes with 0 errors.
- [ ] `npm run build:web` builds production web bundle cleanly.
</verification>

<success_criteria>
- All tasks completed successfully.
- Seamless navigation between Dashboard, Expenses, Categories, and Budgets.
- Custom categories seamlessly available during transaction entry.
- Dynamic filtering and visual threshold progress bars fully integrated and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-categorization-budgeting/04-04-SUMMARY.md`
</output>
