---
phase: 05-dashboards-visualizations
plan: 04
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
files_modified:
  - expense-expert-rn/src/features/dashboard/hooks/useDashboard.ts
  - expense-expert-rn/src/features/dashboard/context/DashboardContext.tsx
  - expense-expert-rn/src/features/dashboard/context/DashboardProvider.tsx
  - expense-expert-rn/src/features/dashboard/index.ts
  - expense-expert-rn/app/(app)/_layout.tsx
  - expense-expert-rn/app/(app)/index.tsx
  - expense-expert-rn/__tests__/features/dashboard/useDashboard.test.ts
  - expense-expert-rn/__tests__/routes/dashboard-screen.test.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "The full Dashboard screen at app/(app)/index.tsx adapts responsively across desktop web (max-w-6xl container, 4-column metric grid, 2-column chart row) and mobile viewports"
    - "useDashboard hook and DashboardProvider coordinate reactive multi-collection queries across expenses, income, savings, loans, and profile"
    - "Users can switch months using MonthNavigator, causing instant recalculation and refresh of summary cards, category donut chart, and recent transactions"
    - "Quick action shortcuts allow instant deep linking to + Add Expense, Budgets, Categories, and Expense List"
    - "Automated end-to-end integration tests verify dashboard rendering, month navigation, metric calculations, and chart interactions"
  artifacts:
    - path: "expense-expert-rn/src/features/dashboard/hooks/useDashboard.ts"
      provides: "Custom hook managing dashboard state, active month, and data loading"
      contains: "export function useDashboard"
    - path: "expense-expert-rn/src/features/dashboard/context/DashboardProvider.tsx"
      provides: "React Context Provider supplying dashboard summary, trend data, and active month state"
      contains: "export const DashboardProvider"
    - path: "expense-expert-rn/app/(app)/index.tsx"
      provides: "Integrated responsive Dashboard screen combining MonthNavigator, SummaryCardsGrid, CategoryDonutChart, MonthlyTrendBarChart, and ActionShortcuts"
      contains: "export default function AppDashboardScreen"
    - path: "expense-expert-rn/__tests__/features/dashboard/useDashboard.test.ts"
      provides: "Unit test suite for useDashboard hook state lifecycle and month switching"
      contains: "describe('useDashboard'"
    - path: "expense-expert-rn/__tests__/routes/dashboard-screen.test.tsx"
      provides: "End-to-end integration tests for the full Dashboard screen layout and user interactions"
      contains: "describe('Dashboard Screen Integration'"
  key_links:
    - from: "expense-expert-rn/app/(app)/index.tsx"
      to: "expense-expert-rn/src/features/dashboard/index.ts"
      via: "MonthNavigator, SummaryCardsGrid, CategoryDonutChart, MonthlyTrendBarChart, ActionShortcuts, useDashboard"
      pattern: "useDashboard"
    - from: "expense-expert-rn/app/(app)/_layout.tsx"
      to: "expense-expert-rn/src/features/dashboard/context/DashboardProvider.tsx"
      via: "Wrapping route hierarchy with DashboardProvider"
      pattern: "<DashboardProvider>"
---

<objective>
Assemble and integrate the complete, responsive Dashboard screen (`app/(app)/index.tsx`) for Expense Expert (DASH-01, DASH-02, DASH-03). Create `useDashboard` hook, `DashboardContext`, and `DashboardProvider`, integrate `MonthNavigator`, `SummaryCardsGrid`, `CategoryDonutChart`, `MonthlyTrendBarChart`, `ActionShortcuts`, and recent transactions with responsive desktop/mobile layouts, and implement comprehensive automated test suites.

Purpose: Deliver the unified analytics command center providing clear financial insights and fast expense management workflows.
Output: useDashboard hook, DashboardProvider, integrated Dashboard screen, and end-to-end integration test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboards-visualizations/05-RESEARCH.md
@.planning/phases/05-dashboards-visualizations/05-VALIDATION.md
@expense-expert-rn/src/features/dashboard/types/dashboard.types.ts
@expense-expert-rn/src/features/dashboard/services/dashboard.service.ts
@expense-expert-rn/src/features/dashboard/components/SummaryCardsGrid.tsx
@expense-expert-rn/src/features/dashboard/components/MonthNavigator.tsx
@expense-expert-rn/src/features/dashboard/components/CategoryDonutChart.tsx
@expense-expert-rn/src/features/dashboard/components/MonthlyTrendBarChart.tsx
@expense-expert-rn/app/(app)/index.tsx
@expense-expert-rn/app/(app)/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement useDashboard hook, DashboardContext, DashboardProvider, and barrel exports</name>
  <files>expense-expert-rn/src/features/dashboard/hooks/useDashboard.ts, expense-expert-rn/src/features/dashboard/context/DashboardContext.tsx, expense-expert-rn/src/features/dashboard/context/DashboardProvider.tsx, expense-expert-rn/src/features/dashboard/index.ts, expense-expert-rn/__tests__/features/dashboard/useDashboard.test.ts</files>
  <read_first>expense-expert-rn/src/features/dashboard/services/dashboard.service.ts, expense-expert-rn/src/features/auth/hooks/useAuth.ts, .planning/phases/05-dashboards-visualizations/05-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `src/features/dashboard/context/DashboardContext.tsx`:
   - Interface `DashboardContextType`:
     - `activeMonth: string`
     - `setActiveMonth: (month: string) => void`
     - `summary: MonthSummary | null`
     - `trends: MonthlyTrend[]`
     - `breakdowns: CategoryBreakdown[]`
     - `isLoading: boolean`
     - `isRefreshing: boolean`
     - `error: string | null`
     - `refresh: () => Promise<void>`

2. Create `src/features/dashboard/context/DashboardProvider.tsx`:
   - Default `activeMonth` initializes to `format(new Date(), 'yyyy-MM')`.
   - Subscribes to authenticated user UID from `useAuth()`.
   - Loads `summary`, `trends`, and `breakdowns` via `DashboardService` when `user.uid` or `activeMonth` changes.
   - Provides optimistic cache hydration and pull-to-refresh / manual refresh capability.

3. Create `src/features/dashboard/hooks/useDashboard.ts`:
   - Re-exports hook consuming `DashboardContext`.
   - Throws error if used outside `DashboardProvider`.

4. Create `src/features/dashboard/index.ts`:
   - Barrel export for types, service, utils, context, provider, hooks, and components (`SummaryCard`, `SummaryCardsGrid`, `MonthNavigator`, `ActionShortcuts`, `CategoryDonutChart`, `MonthlyTrendBarChart`).

5. Create `__tests__/features/dashboard/useDashboard.test.ts`:
   - Mock `DashboardService`.
   - Test hook returns current month summary, trends, and category breakdown.
   - Test `setActiveMonth` triggers reload of month summary and breakdown for the newly selected month.
   - Test error handling and refresh state transitions.
  </action>
  <verify>
    Run `npm test -- __tests__/features/dashboard/useDashboard.test.ts` to verify hook tests pass.
  </verify>
  <acceptance_criteria>
    - `DashboardProvider` manages reactive data loading across summary, trends, and category breakdowns.
    - `useDashboard` exposes clean context API with month mutation and refresh methods.
    - All tests in `useDashboard.test.ts` pass.
  </acceptance_criteria>
  <done>Dashboard state provider and custom hook implemented with unit test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the full responsive Dashboard screen and update route layout</name>
  <files>expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/app/(app)/index.tsx</files>
  <read_first>expense-expert-rn/app/(app)/index.tsx, expense-expert-rn/app/(app)/_layout.tsx, expense-expert-rn/src/features/dashboard/index.ts, .planning/phases/05-dashboards-visualizations/05-RESEARCH.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Update `app/(app)/_layout.tsx`:
   - Wrap the stack navigator with `<DashboardProvider>` inside `BudgetProvider` / `CategoryProvider` hierarchy.

2. Update `app/(app)/index.tsx`:
   - Consume `useDashboard()` along with `useAuth()`, `useExpenses()`, `useCategories()`, and `useBudgets()`.
   - Container structure:
     - Root: `SafeAreaView` with `bg-slate-50 dark:bg-slate-900 flex-1`.
     - `ScrollView` with responsive container: `w-full max-w-6xl mx-auto px-4 py-6 space-y-6`.
   - Section 1: **Dashboard Header & Month Switcher**
     - Welcome header with brand badge `EE`, user name, email, and Sign out button.
     - `MonthNavigator`: `activeMonth={activeMonth}`, `onChangeMonth={setActiveMonth}`.
   - Section 2: **Offline Sync Banner**
     - Renders `OfflineSyncBanner` when pending transactions exist or device is offline.
   - Section 3: **Summary Metrics Grid**
     - Renders `SummaryCardsGrid`:
       - Total Income, Total Expenses, Net Remaining, Total Savings, Loans Taken.
       - Tapping cards routes to `/expenses` or related modules.
   - Section 4: **Visualizations Grid (Desktop 2-Col / Mobile Stack)**
     - Responsive container: `w-full flex-col lg:flex-row gap-6 mb-6`.
     - Left column (`flex-1`): `CategoryDonutChart data={breakdowns}`.
     - Right column (`flex-1`): `MonthlyTrendBarChart trends={trends}`.
   - Section 5: **Action Shortcuts & Budget Progress Widget**
     - `ActionShortcuts`: Fast triggers for `+ Add Expense` (`/expenses/new`), `🎯 Budgets` (`/budgets`), `🏷️ Categories` (`/categories`).
     - Compact Monthly Budget progress widget.
   - Section 6: **Recent Transactions & Multi-criteria Search/Filter**
     - Filtered expense list with view modes (List / Grid / Grouped), category badges, and quick links to expense details (`/expenses/[id]`).
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify type safety and layout compilation.
  </verify>
  <acceptance_criteria>
    - `app/(app)/_layout.tsx` wraps stack with `DashboardProvider`.
    - `app/(app)/index.tsx` incorporates `MonthNavigator`, `SummaryCardsGrid`, `CategoryDonutChart`, `MonthlyTrendBarChart`, `ActionShortcuts`, budget widget, and recent transactions within a responsive desktop/mobile container (`max-w-6xl`).
  </acceptance_criteria>
  <done>Full responsive Dashboard screen assembled with complete visual analytics integration.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dashboard screen integration test suite and verify full validation contract</name>
  <files>expense-expert-rn/__tests__/routes/dashboard-screen.test.tsx</files>
  <read_first>expense-expert-rn/app/(app)/index.tsx, .planning/phases/05-dashboards-visualizations/05-VALIDATION.md</read_first>
  <action>
Inside `expense-expert-rn/`:

1. Create `__tests__/routes/dashboard-screen.test.tsx`:
   - Mock `expo-router` (`useRouter`, `useLocalSearchParams`).
   - Mock `useAuth`, `useExpenses`, `useCategories`, `useBudgets`, and `DashboardService`.
   - Provide test mock data:
     - Month summary: Total Income $4,500.00, Total Expenses $1,250.00, Net Remaining $2,750.00, Total Savings $500.00.
     - Category breakdown: Food (40%), Transport (25%), Entertainment (20%), Utilities (15%).
     - Monthly trends: 6-month historical expense and savings records.
   - Render `AppDashboardScreen` within test wrappers.
   - Test cases:
     1. Renders welcome banner, user account details, and sign out button.
     2. Renders MonthNavigator with localized active month string.
     3. Renders all 4 summary metric cards with formatted currency strings.
     4. Renders CategoryDonutChart and MonthlyTrendBarChart.
     5. Renders quick action shortcuts (+ Add Expense, Budgets, Categories).
     6. Switching month updates MonthNavigator and calls `DashboardService.getMonthSummary`.
     7. Renders recent transactions list with search and filter controls.

2. Run full validation checks:
   - Run `npm test -- __tests__/features/dashboard/ __tests__/routes/dashboard-screen.test.tsx`.
   - Run full project test suite `npm test`.
   - Run TypeScript typecheck `npx tsc --noEmit`.
   - Run Expo web build `npm run build:web`.
  </action>
  <verify>
    Run `npm test && npx tsc --noEmit && npm run build:web` to verify complete test pass, clean typecheck, and successful web build.
  </verify>
  <acceptance_criteria>
    - `__tests__/routes/dashboard-screen.test.tsx` passes with 100% success.
    - Full Jest test suite passes without regressions.
    - TypeScript typecheck has 0 errors.
    - Web export builds cleanly.
  </acceptance_criteria>
  <done>End-to-end dashboard integration tests created and full phase validation suite verified green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test` passes 100% across all feature suites.
- [ ] `npx tsc --noEmit` completes with 0 errors.
- [ ] `npm run build:web` completes successfully.
</verification>

<success_criteria>
- All tasks completed successfully.
- Full Dashboard screen provides cohesive financial summary cards, donut category chart, 6-month trend bar chart, and month navigation.
- Responsive layout provides optimal user experience on desktop web and mobile screens.
- All unit, hook, component, and screen integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboards-visualizations/05-04-SUMMARY.md`
</output>
