# CONCERNS.md

**Analysis Date:** 2026-08-23

## Technical Debt & Known Issues
- The user has stated a goal to "Convert angular application to react-native mobile and web application". This implies the current Angular architecture will need to be analyzed for reusability (e.g., separating business logic from UI components so it can be moved to React Native).
- The current implementation heavily relies on Angular-specific libraries (`@angular/fire`, `ng2-charts`), which will need React Native equivalents (`react-native-firebase`, `react-native-chart-kit` or similar).

## Performance & Scalability
- Tailwind CSS can grow large if not properly purged/configured, though `postcss` is set up.

<!-- refreshed: 2026-08-23 -->
*Generated by codebase analysis: 2026-08-23*
