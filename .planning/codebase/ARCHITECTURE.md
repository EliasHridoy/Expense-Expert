# ARCHITECTURE.md

**Analysis Date:** 2026-08-23

## System Design
- **Single Page Application (SPA)**: Built with Angular 18 using standard client-side routing.
- **Standalone Components**: Usage of Angular 18 features (likely standalone given `app.routes.ts` and `app.config.ts`).
- **Feature-based Architecture**: The `src/app` directory is organized into `core`, `features`, `layout`, and `shared` directories, suggesting a well-structured domain-driven design.
- **Reactive Data Flow**: Uses RxJS for asynchronous data streams, likely paired with Firebase's real-time capabilities.

## Key Layers
- **Core (`src/app/core`)**: Singleton services, guards, interceptors, and application-wide configurations (like Firebase initialization).
- **Features (`src/app/features`)**: Domain-specific modules/components (e.g., expenses, dashboard, reports).
- **Shared (`src/app/shared`)**: Reusable UI components, pipes, and directives.
- **Layout (`src/app/layout`)**: Structural components like headers, sidebars, and navigation.

## Entry Points
- `src/main.ts`: Bootstraps the application using `app.config.ts`.
- `src/app/app.routes.ts`: Defines the primary navigation paths.

<!-- refreshed: 2026-08-23 -->
*Generated by codebase analysis: 2026-08-23*
