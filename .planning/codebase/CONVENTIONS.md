# CONVENTIONS.md

**Analysis Date:** 2026-08-23

## Code Style
- **TypeScript**: Strict mode is likely enabled.
- **Styling**: Tailwind CSS classes are heavily used in HTML templates instead of writing custom CSS/SCSS. Global variables might be in `styles.scss`.
- **Framework APIs**: Utilizing Angular 18 Standalone components, signals (if adopted), and RxJS for reactivity.

## Error Handling
- Global error handling via Angular's `ErrorHandler` might be configured in `core`.
- HTTP and Firebase errors are typically caught and handled via RxJS `catchError` pipelines.

<!-- refreshed: 2026-08-23 -->
*Generated by codebase analysis: 2026-08-23*
